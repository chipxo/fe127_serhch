const Search = () => {
  return (
    <input
      type="text"
      placeholder="Search"
      className="input-neutral input input-bordered hidden w-full max-w-xs sm:flex"
    />
  );
};

export default Search;
